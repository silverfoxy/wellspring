{
  "name": "wellspring",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler dev",
    "deploy": "astro build && wrangler deploy",
    "astro": "astro",
    "cf-typegen": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply wellspring --local",
    "db:migrate:remote": "wrangler d1 migrations apply wellspring --remote"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.7",
    "astro": "^5.7.0",
    "nanoid": "^5.0.9"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@types/node": "^25.7.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.40.0"
  }
}
